ANTHROPIC_API_KEY=your-api-key-here

# Only this origin may call /api/triage from a browser (CORS). Defaults to
# the portfolio's real domain in app.py - override for local testing against
# a site served from a different origin.
# ALLOWED_ORIGIN=http://127.0.0.1:5500
